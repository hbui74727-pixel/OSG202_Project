#ifndef DRAW_PLAYER_H
#define DRAW_PLAYER_H

#include "../types.h"
#include "../renderer.h"

void DrawPlayer_Character(void);  // vẽ sprite + HP bar + tool label
void DrawPlayer_Hotbar(void);     // vẽ hotbar dưới màn hình

#endif
